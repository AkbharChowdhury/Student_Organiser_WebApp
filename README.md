# Student_Organiser_WebApp
 
Deployment limitations: Some features, including CAPTCHA validation and file uploads, behaved differently when deployed to the university web server due to server restrictions. These features worked correctly in the local development environment but required additional server configuration to fully support production deployment.
